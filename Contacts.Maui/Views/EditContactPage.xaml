<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="Contacts.Maui.Views.EditContactPage"
             Title="Edit Contact">
    <!--<VerticalStackLayout>
        <Label 
            x:Name="lblName"
            Text="Welcome to .NET MAUI!"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />

        <Button x:Name="BtnCancel" Text="Cancel" Clicked="BtnCancel_OnClicked"></Button>
    </VerticalStackLayout>-->

    <VerticalStackLayout Spacing="10" Margin="20, 20, 20, 0">

        <Frame Padding="10, 2, 10, 2">
            <HorizontalStackLayout Spacing="10" Padding="10">
                <Label Text="Name:" VerticalOptions="Center" WidthRequest="100"/>
                <Entry x:Name="entryName" VerticalOptions="Center" HorizontalOptions="FillAndExpand">
                    <Entry.Behaviors>
                        <toolkit:TextValidationBehavior
                            x:Name="nameValidator"
                            MinimumLength="1"
                            Flags="ValidateOnAttaching, ValidateOnValueChanged"
                            />
                    </Entry.Behaviors>
                </Entry>
            </HorizontalStackLayout>
        </Frame>

        <Frame Padding="10,2,10,2">
            <HorizontalStackLayout Spacing="10" Padding="10">
                <Label Text="Email:" VerticalOptions="Center" WidthRequest="100"/>
                <Entry x:Name="entryEmail" VerticalOptions="Center" HorizontalOptions="FillAndExpand">
                    <Entry.Behaviors>
                        <toolkit:MultiValidationBehavior
                            x:Name="emailValidator"
                            Flags="ValidateOnAttaching, ValidateOnValueChanged">

                            <toolkit:TextValidationBehavior
                                    MinimumLength="1"
                                    Flags="ValidateOnAttaching, ValidateOnValueChanged"
                                    toolkit:MultiValidationBehavior.Error="Email is required."
                                    />
                            <toolkit:EmailValidationBehavior
                                    Flags="ValidateOnAttaching, ValidateOnValueChanged"
                                    toolkit:MultiValidationBehavior.Error="Email Format is invalid."
                                    />
                        </toolkit:MultiValidationBehavior>
                    </Entry.Behaviors>
                </Entry>
            </HorizontalStackLayout>
        </Frame>

        <Frame Padding="10,2,10,2">
            <HorizontalStackLayout Spacing="10" Padding="10">
                <Label Text="Phone:" VerticalOptions="Center" WidthRequest="100"/>
                <Entry x:Name="entryPhone" VerticalOptions="Center" HorizontalOptions="FillAndExpand"/>
            </HorizontalStackLayout>
        </Frame>

        <Frame Padding="10,2,10,2">
            <HorizontalStackLayout Spacing="10" Padding="10">
                <Label Text="Address:" VerticalOptions="Center" WidthRequest="100"/>
                <Entry x:Name="entryAddress" VerticalOptions="Center" HorizontalOptions="FillAndExpand"/>
            </HorizontalStackLayout>
        </Frame>

        <Button x:Name="BtnUpdate" Text="SaveUpdate" Clicked="BtnUpdate_OnClicked"></Button>
        <Button x:Name="BtnCancel" Text="Cancel" Clicked="BtnCancel_OnClicked"></Button>

    </VerticalStackLayout>
</ContentPage>